"""
External data sources for the display apps.

App-agnostic, like `display/` — an integration knows how to fetch and normalise
data from somewhere on the internet, and knows nothing about screens or panels.
Apps consume these through their own `data.py` seam.
"""
