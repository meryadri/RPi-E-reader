# Ignore everything in this directory.
#
# Deliberately a wholesale ignore rather than per-file entries: the OAuth client
# file Google hands you is named
#   client_secret_918273645-abc.apps.googleusercontent.com.json
# so an entry for "credentials.json" would not match it and you would commit a
# client secret on the first `git add -A`.
*
!.gitignore
